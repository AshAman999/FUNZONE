class Person {
  String name, imgurl, about, email, id;
  List<Person> friends;
  List<Person> blocked;
  
  Person(this.name, this.about, this.email, this.id, this.imgurl, this.friends,this.blocked);
}
